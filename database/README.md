# MySQL setup

1. Start Apache and MySQL in XAMPP.
2. Open `http://localhost/phpmyadmin`.
3. Select **Import** and import `schema.sql` from this folder.
4. Open `http://localhost/qr-system/`.

The default XAMPP MySQL connection is used: user `root`, blank password. Change `api/config.php` if your MySQL password is different.
